"""LangGraph-based agentic shell — optional layer on top of the deterministic core.

Activates only when ANTHROPIC_API_KEY is present.
The agents add explanation and adoption value; they never produce, modify,
or influence a score, a speed value, or a ranking.
"""
